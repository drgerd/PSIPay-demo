# Copy to scripts/env.local (not committed)
#
# AWS credentials: prefer using an AWS named profile or `aws configure`.
# If you must use env vars, set them in your shell/session, not in git.

# AWS_REGION=eu-central-1
# AWS_PROFILE=default

# Backend
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-1.5-flash-latest
